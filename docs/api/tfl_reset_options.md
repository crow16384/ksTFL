# tfl_reset_options()

Purpose: Reset session options to package defaults.

Direct package callees:
- assigns `.options_env$settings <- .options_env$defaults`

Usage notes:
- Returns settings invisibly.
