# add_footer(spec = NULL, ..., level = NULL)

Purpose: Add footer rows to a spec or global options (S3 dispatch).

Direct package callees:
- `UseMethod`, `checkmate::assert_class`, `cli_abort`

Usage notes: Mirrors `add_header` behavior for `spec$footers` or options.
