# This citation is used by utils::citation("ksTFL") and pkgdown's authors page.
# `meta` is supplied by utils::readCitationFile() when the package is not yet
# installed, so avoid re-reading package metadata here.

utils::bibentry(
  bibtype = "Manual",
  title = "ksTFL: Framework for Clinical Tables, Figures, and Listings",
  author = c(
    utils::person("Igor", "Aleschenkov"),
    utils::person("Vladimir", "Larchenko")
  ),
  year = format(Sys.Date(), "%Y"),
  note = sprintf("R package version %s", meta[["Version"]]),
  url = meta[["URL"]]
)
