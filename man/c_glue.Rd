% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rowstyle_actions.R
\name{c_glue}
\alias{c_glue}
\title{Concatenate a Value to Cell Text in Conditional Rows}
\usage{
c_glue(cols, position, glue_col = NULL, text = NULL, separator = NULL)
}
\arguments{
\item{cols}{Tidyselect expression for the target columns
(e.g., \code{col1}, \code{c(col1, col2)}, \code{starts_with("x")}).
Must resolve to visible (non-hidden) report columns only.}

\item{position}{Character. Concatenation side: \code{"before"} prepends the glue
value to the existing cell text; \code{"after"} appends it.}

\item{glue_col}{Optional. Unquoted column name whose formatted value is
concatenated onto the target cells. The column may be hidden (not in the
visible column list). Mutually exclusive with \code{text}.}

\item{text}{Optional. A single literal character string to concatenate onto
the target cells. Mutually exclusive with \code{glue_col}.}

\item{separator}{Character string inserted between the existing cell text and
the glued value when both sides are non-empty. Defaults to \code{""} (direct
concatenation). When either side is empty, no separator is inserted.}
}
\value{
Quosure-style marker (internal use within \code{compute_cols()})
}
\description{
Declares a glue action to concatenate a value — from a data column or a
literal string — to the display text of specified cells in rows matching
the parent \code{compute_cols()} condition.
}
\details{
Must be called inside \code{compute_cols()}.

\strong{Constraints:}
\itemize{
\item Exactly one of \code{glue_col} or \code{text} must be provided.
\item \code{cols} resolves only visible report columns via tidyselect.
\item \code{glue_col} can reference any data column, including hidden ones.
\item \code{glue_col} must not overlap with \code{cols}.
}

\strong{Behavior:}
\itemize{
\item When the glue value (from \code{glue_col} or \code{text}) is empty or \code{NA},
the action is silently skipped for that row/cell.
\item When a target cell was suppressed by deduplication (\code{dedupe = TRUE}
on that column), the glue is silently skipped to preserve the visual
suppression of repeated values.
\item When a target cell is suppressed by a concurrent \code{c_merge()} action
(i.e., it is a non-leader merged cell), the glue is silently skipped.
\item The merge leader cell is glued normally when \code{c_glue()} targets a
column involved in \code{c_merge()} as the first column.
\item Multiple \code{c_glue()} calls on the same column accumulate in call order.
}

\strong{Interaction with other actions:}
\itemize{
\item \code{c_style()}: Fully compatible — styling and text modification are independent.
\item \code{c_merge()}: Compatible. Glue is processed after merge in the renderer.
Non-leader (suppressed) merge cells are skipped; the merge-leader cell is
glued normally.
\item \code{c_addrow()}: \strong{Stackable} — when used together in the same \code{compute_cols()},
\code{c_addrow()} sees glued values. This allows building compound cell values
(e.g., "PARAM: VISIT") before using them in inserted rows.
\item \code{c_pageBreak()}: Fully compatible.
}

\strong{Stackable Actions:}
Actions within a single \code{compute_cols()} call execute \strong{sequentially in the order
specified}. This means \code{c_glue()} modifications are visible to subsequent \code{c_addrow()}
actions in the same call, enabling complex multi-step transformations.
}
\examples{
\dontrun{
  # Append a unit from a hidden column (e.g. unit_col is not in spec cols)
  spec <- compute_cols(spec, !is.na(value),
    c_glue(value, "after", glue_col = unit, separator = " "))

  # Prepend a literal marker to a label column
  spec <- compute_cols(spec, is_total,
    c_glue(label, "before", text = ">> "))

  # Combine with c_style() — independent operations
  spec <- compute_cols(spec, firstOf(group),
    c_glue(label, "before", text = "> "),
    c_style(label, styleRef = "bold"))
  
  # Stackable with c_addrow() — addrow sees glued values
  spec <- compute_cols(spec, PARAM == "ALT",
    c_glue(PARAM, "after", glue_col = VISIT, separator = ": "),
    c_addrow("above", value_from = PARAM)  # Uses "ALT: Week 2"
  )
}

}
\seealso{
\code{\link[=compute_cols]{compute_cols()}}, \code{\link[=c_style]{c_style()}}, \code{\link[=c_merge]{c_merge()}}, \code{\link[=c_addrow]{c_addrow()}}
}
