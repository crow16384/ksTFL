% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report_writer.R
\name{save_report}
\alias{save_report}
\title{Save TFL Report to JSON with Data Files}
\usage{
save_report(
  report,
  docFileName,
  outDir = NULL,
  metaPath = NULL,
  prettify = FALSE,
  insertTOC = NULL,
  tocTitle = NULL
)
}
\arguments{
\item{report}{A TFL_report object (output from \code{create_report()})}

\item{docFileName}{Character string. Name of the rendered document file that will be
created by the C++ renderer (e.g., "report.docx"). This value is stored in the
\verb{_metadata/docFileName} property of the exported JSON spec.}

\item{outDir}{Character string. Output directory where the C++ renderer will save
the rendered document. This path is stored in the \verb{_metadata/outDir} property.
If not provided, defaults to \code{tfl_get_option("output_directory")}.}

\item{metaPath}{Character string. Directory where this function will save the JSON spec
and associated data files. If not provided, a temporary directory is created.
Default: \code{tempdir()}}

\item{prettify}{Logical. If TRUE, format JSON output with indentation and line breaks
for readability. Default: FALSE (compact format)}

\item{insertTOC}{Logical. When \code{TRUE} the renderer prepends a Table of Contents
page (using a \verb{\{ TOC \\f \\h \\z \}} field) before the first spec. Requires at least
one \code{add_title()} or \code{add_subtitle()} call with \code{toclevel} set. Defaults to
the \code{insertTOC} package option (see \code{tfl_set_options()}). Default \code{FALSE}.}

\item{tocTitle}{Character. Heading text placed above the TOC field on the TOC page.
Defaults to the \code{tocTitle} package option (default \code{"Table of Contents"}).
Set to \code{""} to omit the heading.}
}
\value{
Invisibly returns a list with:
\itemize{
\item \code{spec_file}: Name of the saved spec JSON file (hash-based filename)
\item \code{datetime}: Timestamp when the report was saved (ISO 8601 format)
\item \code{metaPath}: Directory where files were saved
}
}
\description{
Serializes a TFL_report object to JSON format along with associated data files
(for tables) or figure files (for figures). Validates the report structure,
processes each specification by its docType, and writes all outputs to disk.
}
\details{
The function performs the following steps:
\enumerate{
\item Validates input is a TFL_report object
\item Serializes the report using \code{serialize_spec()} to validate against schema
\item Processes each specification by docType:
\item{Text:} Drops \code{.metadata} (no additional files needed)
\item{Table:} Extracts data, filters to included columns, saves as JSON
\item{Figure:} Copies image file with original extension preserved
\item Creates \verb{_metadata} section with \code{outDir} and \code{docFileName}
\item Serializes the complete fixed object to JSON
\item Returns information about saved files
}

Error Conditions:
\itemize{
\item Report class is not TFL_report
\item Table/Figure spec has \code{hasData=TRUE} but no actual data available
\item dataRef has multiple entries (currently only single file references supported)
\item File I/O errors when writing JSON or copying files
}
}
\examples{
\dontrun{
spec1 <- create_table(mtcars)
spec2 <- create_text()
report <- create_report(spec1, spec2)

# Save with explicit parameters
result <- save_report(
  report,
  docFileName = "report.docx",
  outDir = "/output/path",
  metaPath = "/meta/path"
)

# Save with defaults (uses tempdir and tfl_options)
result <- save_report(report, docFileName = "report.docx")

cat("Spec saved as:", result$spec_file, "\n")
cat("Saved to:", result$metaPath, "\n")

# Generate a report with an auto-populated TOC page
t1 <- create_table(adsl) |>
  add_title("Table 1: Demographics", toclevel = 1) |>
  set_document(hasData = TRUE)

t2 <- create_table(advs) |>
  add_title("Table 2: Vital Signs by Visit", toclevel = 1) |>
  add_subtitle("#ByGroup1", toclevel = 2) |>
  set_document(hasData = TRUE)

report <- create_report(t1, t2)
save_report(
  report,
  docFileName  = "tables.docx",
  outDir       = "output/",
  insertTOC    = TRUE,
  tocTitle     = "List of Tables"
)
# Open tables.docx in Word, click the TOC placeholder, press F9 to populate it.
}
}
