% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rowstyle_actions.R
\name{compute_cols}
\alias{compute_cols}
\title{Define Conditional Row Actions for Tables}
\usage{
compute_cols(spec, cond, ...)
}
\arguments{
\item{spec}{A TFL_spec object (must have docType = "Table")}

\item{cond}{An unquoted logical expression to be evaluated in the data environment.
Can reference:
\itemize{
\item Data columns directly (e.g., \code{col1 > 10}, \code{Parameter == "Pulse"})
\item Helper functions (e.g., \code{firstOf()}, \code{lastOf()}, \code{uniqueOf()})
}
Must return a logical vector of length equal to \code{nrow(data)}.}

\item{...}{Action function calls: \code{c_style()}, \code{c_merge()}, \code{c_addrow()}, \code{c_glue()}, \code{c_clear()}.
Multiple actions allowed, including duplicates. Actions are captured unevaluated.}
}
\value{
Modified \code{spec} object with appended action metadata in \code{spec$.metadata$compute_cols}.
Invisibly returns the updated spec to enable piping workflows.
}
\description{
Declares a set of styling, merging, and row insertion actions to be applied
to rows matching a condition. Actions are captured as unevaluated expressions
and evaluated later during \code{create_report()}. Supports complex conditions using
data columns and helper functions.
}
\details{
\strong{Execution Timeline:}
\enumerate{
\item \code{compute_cols()} captures condition and actions (no evaluation)
\item Appends to \code{spec$.metadata$compute_cols} list
\item During \code{create_report()}, conditions are evaluated and matched rows identified
\item Actions are applied to matching rows (styling, merging, row insertion)
\item StyleRows are serialized to JSON
}

\strong{Constraints:}
\itemize{
\item Only allowed for docType = "Table"
\item Multiple \code{compute_cols()} calls accumulate on the same spec
\item Actions on the same row from different \code{compute_cols()} blocks are aggregated
\item Conditions must be deterministic (no NA values allowed)
}

\strong{Action Functions} (used inside \code{compute_cols()}):
\itemize{
\item \code{c_style(cols, styleRef)}: Apply style(s) to columns in matching rows
\item \code{c_merge(cols, styleRef = NULL)}: Merge adjacent columns in matching rows
\item \code{c_addrow(pos, value_from = NULL, styleRef = NULL)}: Insert row above/below matching rows
\item \code{c_pageBreak()}: Insert a page break at the matching row (no args)
\item \code{c_glue(cols, position, glue_col = NULL, text = NULL, separator = NULL)}: Concatenate a data column value or literal text to matching cell text
\item \code{c_clear(cols)}: Clear the display text of matching cells (render as blank)
}
}
\examples{
\dontrun{
  spec <- create_table(mtcars)
  spec <- add_style(spec, id = "bold", s_font(bold = TRUE))
  spec <- add_style(spec, id = "red", s_font(color = "red"))
  spec <- add_style(spec, id = "highlight", s_table_style(background_color = "yellow"))

  # Style columns in rows where cyl is first occurrence
  spec <- compute_cols(spec, firstOf(cyl), c_style(c(mpg, hp), styleRef = "bold"))

  # Style and merge columns in rows with high hp
  spec <- compute_cols(spec, hp > 200, 
    c_style(hp, styleRef = "red"),
    c_merge(c(wt, qsec), styleRef = "highlight")
  )

  # Add empty separator row above first occurrence
  spec <- compute_cols(spec, firstOf(cyl), c_addrow(pos = "above"))

  # Add row with content from a column
  spec <- compute_cols(spec, lastOf(cyl), c_addrow(pos = "below", value_from = "mpg"))
}

}
\seealso{
\code{\link[=c_style]{c_style()}}, \code{\link[=c_merge]{c_merge()}}, \code{\link[=c_addrow]{c_addrow()}} for action functions used within \code{compute_cols()}
}
