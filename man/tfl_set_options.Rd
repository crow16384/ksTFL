% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pkg_settings.R
\name{tfl_set_options}
\alias{tfl_set_options}
\title{Update the session package settings}
\usage{
tfl_set_options(
  ...,
  bodyTitles = NULL,
  bodySubtitles = NULL,
  bodyFootnotes = NULL,
  isContinues = NULL,
  contentWidth = NULL,
  missings = NULL,
  autoColWidth = NULL,
  minColWidth = NULL,
  output_directory = "."
)
}
\arguments{
\item{...}{Named arguments OR settings objects returned from helper constructors.
\itemize{
\item Named scalar options (e.g. \code{bodyTitles = FALSE}, \code{contentWidth = "95\%"}).
\item Settings objects produced by helper constructors such as \code{add_header()}, \code{add_footer()}, \code{add_style()}, \code{add_body_text()}, and \verb{set_page_style(}p_page(\code{p_margins()})\verb{)}.
\item A mixture of both named values and settings objects is accepted; the function routes each into the appropriate internal slot.
}}

\item{bodyTitles}{Logical; override whether body titles are shown.}

\item{bodySubtitles}{Logical; override whether body subtitles are shown.}

\item{bodyFootnotes}{Logical; override whether body footnotes are shown.}

\item{isContinues}{Logical; override continuation behavior.}

\item{contentWidth}{Character; width for content area (e.g. "100\%", "95\%").}

\item{missings}{Character; default representation for missing values (e.g. "NA", ".", "---").}

\item{autoColWidth}{Logical; enable automatic column width recalculation when user sets \code{colWidth} via \code{define_cols()}.
Default TRUE. When TRUE, locked columns maintain exact width while unlocked columns normalize to fill remaining space.
Set FALSE to disable auto-recalculation and manage widths manually.}

\item{minColWidth}{Numeric; minimum relative column width (\%) for unlocked columns during recalculation.
Default 0.5. Used to validate that relative widths don't squeeze columns below acceptable minimum.}

\item{output_directory}{Character; path to default output directory of rendered document.}
}
\value{
The updated settings list, returned invisibly. Use \code{tfl_get_options()} to inspect.
}
\description{
Update ksTFL session options. This function accepts:
\itemize{
\item{Named scalar options (e.g. \code{bodyTitles = FALSE}, \code{contentWidth = "95\%"}).}
\item{Settings objects produced by helper constructors such as \code{add_header()}, \code{add_footer()}, \code{add_body_text()}, or page objects from \code{p_page()}.}
\item{A mixture of both named values and settings objects.}
}
}
\details{
The function tries to intelligently route each supplied object into the
appropriate internal settings slot (headers, footers, styles, bodyText, page).
}
\examples{
\dontrun{
# Set a named option
tfl_set_options(bodyTitles = FALSE, contentWidth = "95\%", missings = ".")

# Update page style via helper
 tfl_set_options(
   set_page_style(page= p_page(
   size = "Letter",
   orientation = "portrait",
   margins = p_margins(top = "1in", bottom = "1in", left = "0.75in", right = "0.75in")
 )))

# Add default header and footer via helpers
tfl_set_options(
  add_header(c("Left Header", "Center Header", "Right Header")),
  add_footer(c("Left Footer", "Center Footer", "Right Footer"))
)

# Add default body text via helper
tfl_set_options(
  add_body_text("This is the default body text for all text specs.")
)

# Control automatic column width recalculation
# Enable auto-recalculation (default):
tfl_set_options(autoColWidth = TRUE)
spec <- create_table(data) |>
  define_cols("id", colWidth = "20\%")  # Locks id, others auto-adjust

# Disable auto-recalculation for manual width management:
tfl_set_options(autoColWidth = FALSE)
spec <- create_table(data) |>
  define_cols(c("id", "age"), colWidth = c("20\%", "30\%"))  # Exact widths, no auto-adjust
}
}
