% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rowstyle_actions.R
\name{c_addrow}
\alias{c_addrow}
\title{Insert Additional Row in Conditional Rows}
\usage{
c_addrow(pos, value_from = NULL, styleRef = NULL)
}
\arguments{
\item{pos}{Character. Position for insertion: "above" or "below".}

\item{value_from}{Character or unquoted column name. Optional source column
for the inserted row's content. If NULL or missing, creates an empty separator row.}

\item{styleRef}{Character. Optional style to apply to the inserted row.
If NULL, no special styling. Can be a single style or \code{f_combine()} result.}
}
\value{
Quosure structure (internal use within \code{compute_cols()})
}
\description{
Declares an additional row to be inserted above or below rows matching
the parent \code{compute_cols()} condition. Content can optionally be copied from a
specified column; if omitted, creates an empty separator row.
}
\details{
Must be called inside \code{compute_cols()}.

\strong{Behavior:}
\itemize{
\item Multiple \code{c_addrow()} calls in one \code{compute_cols()} accumulate
\item Order of appearance is preserved
\item Coexists with other actions on same row
\item If \code{value_from} is provided, must exist in spec columns (data_env reference)
\item If \code{value_from} is NULL or missing, creates an empty separator row
}
}
\examples{
\dontrun{
  compute_cols(spec, lastOf(treatment), c_addrow(pos = "below", value_from = "treatment"))
  compute_cols(spec, firstOf(visit), c_addrow(pos = "above"))
}

}
\seealso{
\code{\link[=compute_cols]{compute_cols()}} for conditional row actions, \code{\link[=c_style]{c_style()}}, \code{\link[=c_merge]{c_merge()}} for other action types
}
