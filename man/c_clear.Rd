% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rowstyle_actions.R
\name{c_clear}
\alias{c_clear}
\title{Clear Cell Content in Conditional Rows}
\usage{
c_clear(cols)
}
\arguments{
\item{cols}{Tidyselect expression for the target columns to blank
(e.g., \code{col1}, \code{c(col1, col2)}, \code{starts_with("x")}).
Must resolve to visible (non-hidden) report columns only.}
}
\value{
Quosure-style marker (internal use within \code{compute_cols()})
}
\description{
Declares a clear action that blanks the display text of specified cells in
rows matching the parent \code{compute_cols()} condition. The cells are rendered
empty without affecting their column structure, width, or styling.
}
\details{
Must be called inside \code{compute_cols()}.

\strong{Behavior:}
\itemize{
\item Sets the rendered cell text to \code{""} for matching rows.
\item Processed before \code{c_merge()} and \code{c_glue()} in the rendering chain,
so the cleared state participates in subsequent actions. In particular:
combining \code{c_clear()} + \code{c_glue()} on the same column effectively
\emph{replaces} the original cell content with the glued value.
\item Compatible with \code{c_style()}: styling is independent of text content.
\item When \code{c_merge()} targets a cleared leader cell, the merged span
renders as a blank merged cell.
}
}
\examples{
\dontrun{
  # Blank label column in total rows
  spec <- compute_cols(spec, is_total,
    c_clear(label))

  # Clear then replace with a value from another column (full replacement)
  spec <- compute_cols(spec, condition,
    c_clear(display_col),
    c_glue(display_col, "after", glue_col = replacement_col))
}

}
\seealso{
\code{\link[=compute_cols]{compute_cols()}}, \code{\link[=c_style]{c_style()}}, \code{\link[=c_merge]{c_merge()}}, \code{\link[=c_glue]{c_glue()}}
}
