% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render_docx.R
\name{render_docx}
\alias{render_docx}
\title{Render a DOCX Document from TFL Report}
\usage{
render_docx(
  spec_json,
  template_json = NULL,
  output_path,
  font_dirs = NULL,
  fallback_font = NULL,
  verbose = FALSE
)
}
\arguments{
\item{spec_json}{Character string. Path to the spec JSON file produced by
\code{\link{save_report}}.}

\item{template_json}{Character string. Path to the styles template JSON file.
If \code{NULL} (default), the template is resolved automatically from the
\code{docTemplate} name stored in the spec. The name is looked up in the
package's bundled \code{inst/templates/} directory. If not found, the default
\code{KeyStat_default} template is used and a warning is issued.}

\item{output_path}{Character string. Path for the output .docx file. If the
directory does not exist, it will be created.}

\item{font_dirs}{Character vector (optional). Additional directories to search
for fonts. The package's bundled fonts (inst/fonts/) are always included
automatically.}

\item{fallback_font}{Character string (optional). Path to a fallback font file.
If not specified, the embedded fallback font is used.}

\item{verbose}{Logical. If \code{TRUE}, print progress messages to stderr.
Default: \code{FALSE}.}
}
\value{
Invisibly returns the \code{output_path} (the path to the generated .docx file).
}
\description{
Renders a TFL report (previously saved via \code{\link{save_report}}) into
a styled DOCX document using the C++ rendering engine. The renderer
performs deterministic pagination with HarfBuzz-based text measurement,
producing submission-quality clinical tables, figures, and listings.
}
\examples{
\dontrun{
spec <- create_table(mtcars)
spec <- add_title(spec, "Motor Trend Car Road Tests")
report <- create_report(spec)
saved <- save_report(report, docFileName = "demo.docx")

render_docx(
  spec_json = file.path(saved$metaPath, saved$spec_file),
  output_path = "output/demo.docx"
)
}

}
