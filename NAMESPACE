# Import specific functions to avoid namespace conflicts
importFrom(cli, cli_abort, cli_warn, cli_alert_success, cli_alert_info, cli_text)
importFrom(checkmate, assert_class, assert_string, assert_character, assert_list, assert_data_frame, assert_null, assert_names)
importFrom(jsonlite, toJSON, fromJSON, read_json, write_json)
importFrom(purrr, map, map_chr, map_lgl, map_dbl, map_int, keep, discard)
importFrom(rlang, enquos, quo_get_expr, quo_get_env, eval_tidy, is_call, is_symbol, caller_env, new_environment, new_data_mask, expr, enexpr, "%||%")
importFrom(tidyselect, everything, eval_select)
importFrom(utils, packageVersion, modifyList)

# Export only public functions (those marked with @export in documentation)
export(
  # Initialization function
  tfl_init,
  
  # Style modifier functions
  s_font,
  s_spacing,
  s_indents,
  s_paragraph,
  s_border,
  s_borders,
  s_table_style,
  s_margins,
  s_page,
  
  # Column format function
  c_format,
  
  # Style context manipulation (generic and methods)
  add_style,
  add_style.TFL_spec,
  add_style.default,
  
  # Column definition
  define_cols,
  
  # Content functions (title, subtitle, footnote)
  add_title,
  add_subtitle,
  add_footnote,
  
  # Body text functions (generic and methods)
  add_body_text,
  add_body_text.TFL_spec,
  add_body_text.TFL_options,
  add_body_text.default,
  
  # Header functions (generic and methods)
  add_header,
  add_header.TFL_spec,
  add_header.default,
  
  # Footer functions (generic and methods)
  add_footer,
  add_footer.TFL_spec,
  add_footer.TFL_options,
  add_footer.default,
  
  # Stub column definition
  add_stub_column,
  
  # Document configuration
  set_document,
  set_document_style,
  
  # Specification preview
  preview_spec,
  
  # Package settings functions
  tfl_get_settings,
  tfl_get_setting,
  tfl_set_settings,
  tfl_reset_settings
)
